import os
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'hackaton.settings')

import django
import json
django.setup()

from GettingWise.models import known_vendors, item

def populate():
        #confusing shit
        #code to populate the database
        #get the fata from json file
        
        return 0